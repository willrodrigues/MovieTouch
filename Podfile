# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'MovieTouch' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for MovieTouch
  pod 'RxSwift', '~> 5'
  pod 'RxCocoa', '~> 5'

  target 'MovieTouchTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'MovieTouchUITests' do
    inherit! :search_paths
    # Pods for testing
  end
end
